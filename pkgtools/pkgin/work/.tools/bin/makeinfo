#!/bin/sh
wrapperlog="${TOOLS_WRAPPER_LOG-/work/git/pkgsrc/pkgtools/pkgin/work/.work.log}"
echo "[*] "/work/git/pkgsrc/pkgtools/pkgin/work/.tools/bin/makeinfo" $@" >> $wrapperlog
echo "<.> for arg in "$@"; do case "$arg" in --version) exit 1 ;; esac; done; /work/git/pkgsrc/mk/gnu-config/missing makeinfo "$@"" >> $wrapperlog
for arg in "$@"; do case "$arg" in --version) exit 1 ;; esac; done; /work/git/pkgsrc/mk/gnu-config/missing makeinfo "$@"
